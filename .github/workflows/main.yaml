name: "This repo has x stars"

on:
  push:
  watch:
    types: [started]

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Update description
        with:
          token: ${{ secrets.TOKEN }}
          template: 'test'
